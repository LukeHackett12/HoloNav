using UnityEngine;
using System.Collections;
using System.Linq;

public class WorldCursor : MonoBehaviour
{
    /*
    public GameObject arrow;
    public GameObect destObj;
    private float distance = 3.0f;
    KeywordRecognizer kr;
    Vector3 destPos;

    // Use this for initialization
    void Start()
    {
        arrow = GameObject.FindWithTag("Arrow");
        Dictionary<string, System.Action> keywords = new Dictionary<string, System.Action>();
        keywords.Add("Destination One", () =>
        {
            destObj = GameObject.FindWithTag("Destination1");
            destPos = new Vector3(destObj.transform.position.x, destObj.transform.position.y,
                destObj.transform.position.z);
        });
        Debug.Log(destPos);
        
    }

        // Update is called once per frame
        void Update()
    {
        if (arrow != null)
        {
            arrow.transform.position = Camera.main.transform.position + arrow.transform.forward * distance;
            arrow.transform.rotation = new Quaternion(0.0f, arrow.transform.rotation.y, 0.0f, arrow.transform.rotation.w);
        }
    } */

}